# TTech Style

## Build

```shell
mvn clean install
(cd idea_plugin && gradle buildPlugin)
```

Plugin will be located in `./idea_plugin/build/distributions/idea_plugin.zip`.
Make a new release on github.
